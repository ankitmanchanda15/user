spring:
  datasource: {url: 'jdbc:mysql://localhost:3306/your_database', username: your_username,
    password: your_password}
  jpa:
    hibernate: {ddl-auto: none}
    properties:
      hibernate: {format_sql: true}
  flyway: {enabled: true, url: 'jdbc:mysql://localhost:3306/your_database', user: your_username,
    password: your_password}